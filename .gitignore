# ── Android / Gradle ──────────────────────────────
*.iml
.gradle/
/local.properties
/.idea/
/build/
app/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# ── Firma (NUNCA en el repo; el CI usa el secret KEYSTORE_BASE64) ──
*.jks
*.keystore
keystore.properties

# ── Misc ──────────────────────────────────────────
proyecto.zip
*.zip
.DS_Store
